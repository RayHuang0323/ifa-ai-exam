# 部署指南

部署： npm.cmd run build npm.cmd run deploy

完成條件： - Build Success - Published

GitHub Pages 為正式驗收網站。
# Sprint 24A 同步設定

先依 `docs/google-apps-script/README.md` 部署 Web App，將 `/exec` URL 與可選 WRITE_KEY 寫入本機 `.env` 的 `VITE_PROGRESS_API_URL`、`VITE_PROGRESS_WRITE_KEY` 後再 build/deploy。Google Sheet 不需公開分享，且不使用 Google Auth、OAuth 或 Client ID。

## Sprint 24E

前端部署後，另需將新版 `docs/google-apps-script/Code.gs` 貼回 Apps Script 並重新部署 Web App，首頁 remote-first 摘要才會生效。`COACH_READ_KEY` 不放前端；`OFFICIAL_START_DATE` 可選，未設定時使用 `2026-07-14`。
