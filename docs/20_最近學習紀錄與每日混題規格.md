# 最近學習紀錄與每日混題規格

## Recent Activity

首頁讀取 `ifa-study-progress-v1`，依 completedAt 由新到舊顯示最近 3 筆 StudySession。每筆顯示日期時間、模式、題數、答對／答錯、正確率與花費時間；資料缺漏時使用安全文字，不顯示假資料。

## 每日混題

每日任務維持原有題數。系統從 `getReviewableWrongAnswers()` 依既有優先順序取得約 30% 題目，再用 Question Engine 查詢正式 Week1 題庫；其餘以一般正式題目補足且不得重複。錯題不足或不存在時維持原本一般題流程。

## 規則型提醒

首頁依序處理考前 14 天、高風險錯題、今日已完成仍有待複習錯題與本週進度落後，提供文字提醒。此規則不使用 AI 或外部 API，未來可在不變更 localStorage key 的前提下擴充。
